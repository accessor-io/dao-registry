{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "largestTransaction",
  "description": "Largest treasury transaction value in the time range (reference currency).",
  "type": "object",
  "properties": {
    "key": { "const": "largestTransaction" },
    "label": { "type": "string", "const": "Largest Transaction" },
    "unit": { "type": "string", "const": "currency" },
    "currency": { "type": "string", "default": "USD" },
    "category": { "type": "string", "const": "treasury" },
    "timeRange": { "type": "string", "enum": ["1d", "7d", "30d", "90d", "365d", "all"], "default": "30d" },
    "source": { "type": "object", "properties": { "type": { "enum": ["derived"] } }, "required": ["type"] },
    "computation": { "type": "object", "properties": { "method": { "type": "string", "const": "max(transaction.valueInCurrency) over timeRange" } } }
  },
  "required": ["key", "label", "unit", "category", "source"]
}

