{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "newMembersThisMonth",
  "description": "Number of new members added in the current calendar month.",
  "type": "object",
  "properties": {
    "key": { "const": "newMembersThisMonth" },
    "label": { "type": "string", "const": "New Members This Month" },
    "unit": { "type": "string", "const": "count" },
    "category": { "type": "string", "const": "member" },
    "source": { "type": "object", "properties": { "type": { "enum": ["derived"] } }, "required": ["type"] },
    "computation": { "type": "object", "properties": { "method": { "type": "string", "const": "count(addresses with firstActivity in month)" } } }
  },
  "required": ["key", "label", "unit", "category", "source"]
}

