{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "proposalForVotesPercentage",
  "description": "Percentage of for votes for a proposal.",
  "type": "object",
  "properties": {
    "key": { "const": "proposalForVotesPercentage" },
    "label": { "type": "string", "const": "For Votes %" },
    "unit": { "type": "string", "const": "percentage" },
    "category": { "type": "string", "const": "proposal" },
    "source": { "type": "object", "properties": { "type": { "enum": ["derived"] } }, "required": ["type"] },
    "computation": { "type": "object", "properties": { "method": { "type": "string", "const": "(forVotes / totalVotes) * 100" } } }
  },
  "required": ["key", "label", "unit", "category", "source"]
}

