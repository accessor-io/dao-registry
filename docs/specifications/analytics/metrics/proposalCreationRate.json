{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "proposalCreationRate",
  "description": "Average number of proposals created per member over a time range.",
  "type": "object",
  "properties": {
    "key": { "const": "proposalCreationRate" },
    "label": { "type": "string", "const": "Proposal Creation Rate" },
    "unit": { "type": "string", "const": "count_per_member" },
    "category": { "type": "string", "const": "member" },
    "timeRange": { "type": "string", "enum": ["1d", "7d", "30d", "90d", "365d", "all"], "default": "90d" },
    "source": { "type": "object", "properties": { "type": { "enum": ["derived"] } }, "required": ["type"] },
    "computation": { "type": "object", "properties": { "method": { "type": "string", "const": "totalProposals(timeRange) / totalMembers" }, "dependencies": { "type": "array", "items": { "type": "string" }, "default": ["totalMembers", "totalProposals"] } } }
  },
  "required": ["key", "label", "unit", "category", "source"]
}

